#
An interactive Animated Credit Card built using HTML, CSS, and JavaScript that dynamically updates and rotates based on user input.
This project simulates a real-world credit/debit card experience where the card visually responds when users enter details such as:
Card Number, Card Holder Name, Expiry Date, and CVV.
